<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<!-- Fonts -->
	<FontFamily x:Key="OpenSansRegular">./fonts/OpenSans-Regular.ttf#Open Sans</FontFamily>
	<FontFamily x:Key="OpenSansBold">./fonts/OpenSans-Bold.ttf#Open Sans</FontFamily>
	<FontFamily x:Key="OpenSansSemiBold">./fonts/OpenSans-SemiBold.ttf#Open Sans</FontFamily>
	<FontFamily x:Key="OpenSansExtraBold">./fonts/OpenSans-ExtraBold.ttf#Open Sans</FontFamily>

	<!-- Button Brush -->
	<LinearGradientBrush x:Key="NeonBrush" StartPoint="0,0.5" EndPoint="1,0.5">
		<LinearGradientBrush.GradientStops>
			<GradientStop Color="Black" Offset="0.0"></GradientStop>
			<GradientStop Color="#500096" Offset="0.5"></GradientStop>
			<GradientStop Color="Black" Offset="1"></GradientStop>
		</LinearGradientBrush.GradientStops>
	</LinearGradientBrush>

	<!-- Border Color -->
	<SolidColorBrush x:Key="PurpleBrush" Color="#5A00AA"/>
	<SolidColorBrush x:Key="WhiteBrush" Color="White"/>

	<!-- Button -->
	<Style x:Key="ButtonStyle" TargetType="Button">
		<Setter Property="Foreground" Value="Black"/>
		<Setter Property="Background" Value="White"/>
		<Setter Property="Cursor" Value="Hand"/>
		<Setter Property="FontSize" Value="18"/>
		<Setter Property="FontFamily" Value="{StaticResource OpenSansBold}"/>
		<Setter Property="VerticalAlignment" Value="Stretch"/>
		<Setter Property="HorizontalAlignment" Value="Center"/>
	</Style>

	<!-- New Title -->
	<Style x:Key="TitleStyle" TargetType="{x:Type Label}">
		<Setter Property="Foreground" Value="Black"/>
		<Setter Property="Background" Value="White"/>
		<Setter Property="FontSize" Value="36"/>
		<Setter Property="FontFamily" Value="{StaticResource OpenSansBold}"/>
		<Setter Property="VerticalAlignment" Value="Stretch"/>
		<Setter Property="HorizontalAlignment" Value="Stretch"/>
	</Style>

	<!-- New Label -->
	<Style x:Key="LabelStyle" TargetType="{x:Type Label}">
		<Setter Property="Foreground" Value="Black"/>
		<Setter Property="FontSize" Value="18"/>
		<Setter Property="FontFamily" Value="{StaticResource OpenSansRegular}"/>
		<Setter Property="VerticalAlignment" Value="Center"/>
		<Setter Property="HorizontalAlignment" Value="Center"/>
		<Setter Property="VerticalContentAlignment" Value="Stretch"/>
	</Style>

	<!-- TextBox -->
	<Style x:Key="TextBoxNeon" TargetType="TextBox">
		<Setter Property="Foreground" Value="White"></Setter>
		<Setter Property="FontFamily" Value="{StaticResource OpenSansBold}"></Setter>
		<Setter Property="FontSize" Value="28"></Setter>
		<Setter Property="Width" Value="800"></Setter>
		<Setter Property="Height" Value="60"></Setter>
		<Setter Property="CaretBrush" Value="White"></Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="TextBox">
					<Border x:Name="border" BorderBrush="{StaticResource PurpleBrush}" BorderThickness="2" CornerRadius="5" Background="{StaticResource NeonBrush}">
						<ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" CanContentScroll="False" VerticalAlignment="Center" Margin="5" Cursor="IBeam"></ScrollViewer>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="BorderBrush" TargetName="border" Value="White"></Setter>
						</Trigger>
						<Trigger Property="IsFocused" Value="True">
							<Setter Property="BorderBrush" TargetName="border" Value="{StaticResource PurpleBrush}"></Setter>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<!-- PasswordBox -->
	<Style x:Key="PasswordBoxNeon" TargetType="PasswordBox">
		<Setter Property="Foreground" Value="White"></Setter>
		<Setter Property="FontFamily" Value="{StaticResource OpenSansBold}"></Setter>
		<Setter Property="FontSize" Value="28"></Setter>
		<Setter Property="Width" Value="800"></Setter>
		<Setter Property="Height" Value="60"></Setter>
		<Setter Property="CaretBrush" Value="White"></Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="PasswordBox">
					<Border x:Name="border" BorderBrush="{StaticResource PurpleBrush}" BorderThickness="2" CornerRadius="5" Background="{StaticResource NeonBrush}">
						<ScrollViewer x:Name="PART_ContentHost" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" CanContentScroll="False" VerticalAlignment="Center" Margin="5" Cursor="IBeam"></ScrollViewer>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter Property="BorderBrush" TargetName="border" Value="White"></Setter>
						</Trigger>
						<Trigger Property="IsFocused" Value="True">
							<Setter Property="BorderBrush" TargetName="border" Value="{StaticResource PurpleBrush}"></Setter>
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>
